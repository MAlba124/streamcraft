[package]
name = "sc-mp4"
description = "Plugin for streamcraft backed by the pure-Rust oxideav-mp4 (adoption pending evaluation)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
name = "sc_mp4"

[dependencies]
streamcraft-core = { path = "../core" }
oxideav-mp4 = { version = "0.0.9", default-features = false }

[dev-dependencies]
streamcraft-elements = { path = "../elements" }
