[package]
name = "sc-av1"
description = "Codec plugin for streamcraft backed by the pure-Rust oxideav-av1 (adoption pending evaluation)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
name = "sc_av1"

[dependencies]
streamcraft-core = { path = "../core" }
oxideav-av1 = { version = "0.1", default-features = false }

[dev-dependencies]
streamcraft-elements = { path = "../elements" }
