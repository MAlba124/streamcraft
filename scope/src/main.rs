//! `scraft-scope` — attach-mode inspector binary. Connects to a running streamcraft
//! app over the introspection protocol and renders the Slint UI + serves MCP
//! (spec: Introspection protocol and scraft-scope).

fn main() {
    eprintln!("scraft-scope: not yet implemented — see streamcraft.md: scraft-scope");
    std::process::exit(1);
}
