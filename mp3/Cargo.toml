[package]
name = "sc-mp3"
description = "Plugin for streamcraft backed by the pure-Rust oxideav-mp3 (adoption pending evaluation)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
name = "sc_mp3"

[dependencies]
streamcraft-core = { path = "../core" }
oxideav-mp3 = { version = "0.1", default-features = false }

[dev-dependencies]
streamcraft-elements = { path = "../elements" }
